# Test Support

This folder contains reusable test infrastructure that is not tied to a single
spec file.

- `cdp.js` exposes the shared `test` and `expect` fixtures.
- `visual.js` hides dynamic shop UI and prepares the page for screenshot tests.
- Future shared fixtures, page objects, or browser helpers should live here.
